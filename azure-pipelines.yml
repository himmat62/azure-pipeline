# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

pool:
  vmImage: ubuntu-latest

jobs:
- job: FirstJob
  timeoutInMinutes: 10
  steps:
  - bash: echo "first job triggered "

- job: SecondJob
  timeoutInMinutes: 10
  steps:
  - bash: echo "Second job triggered "

